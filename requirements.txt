# ─────────────────────────────────────────────────────────
# requirements.txt (ROOT) — untuk SELURUH repo:
# training model, simulator, dashboard Streamlit, ingestion MQTT,
# dan app Reflex. Hanya berisi library yang BENAR-BENAR di-import di kode.
#
# Versi dipin ke versi yang SUDAH TERPASANG & TERVERIFIKASI JALAN di
# environment ini (bukan pin aspirational lama yang tidak cocok — mis.
# numpy==2.4.6/torch==2.12.0 yang gagal di-build pada Python 3.14).
# scikit-learn==1.9.0 dipertahankan (cocok dengan model .pkl tersimpan).
# ─────────────────────────────────────────────────────────

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0
# App & dashboard
reflex==0.9.5.post2
streamlit==1.58.0
plotly==6.8.0

# Data & Machine Learning
pandas==2.3.3
numpy==2.3.5
scikit-learn==1.9.0
xgboost==3.2.0
torch==2.10.0
matplotlib==3.10.9

# Storage & integrasi
psycopg2-binary==2.9.12
pyyaml==6.0.3
paho-mqtt==2.1.0

# Utilitas
python-dotenv==1.2.2
requests==2.33.0
filelock==3.29.0
psutil==7.2.2
